{
  "name": "wombcare-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Security-rules unit tests for the WombCare Realtime Database.",
  "scripts": {
    "test": "mocha --timeout 10000 test/**/*.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.35.1",
    "mocha": "^10.8.2"
  }
}
